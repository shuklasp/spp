################################################################################
# Application Runtime Configuration — test_api_app
#
# Values here can be read via:
#   \SPP\App::getAppConf('key_name', 'test_api_app')
################################################################################

app_name: "test_api_app"
app_type: "mixed"
default_layout: "premium-glass"

# Asset directories (relative to app src)
assets:
  theme-assets: "resources/themes"
  app-assets: "assets"

# Uncomment to enable features:
# debug: true
# cache: false
# auth_required: true