## NAME

**delete** - Destructive operations for entities and apps

## PURPOSE

The `delete` namespace is a logical grouping of SPP CLI commands related to Destructive operations for entities and apps. This namespace provides a suite of administrative and operational tools specifically designed to interact with the underlying delete subsystems of the framework.

## UNDER THE HOOD ACTIVITY

When invoking commands within the `delete` namespace, the CLI router isolates execution to specific modules and classes optimized for Destructive operations for entities and apps. This modular architectural grouping prevents command collisions and ensures that each subsystem operates within its dedicated execution context.

To view the exhaustive details, options, and deep functionality of any specific command within this group, execute `php spp.php man delete:<subcommand>`.
