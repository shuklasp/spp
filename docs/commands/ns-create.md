## NAME

**create** - Scaffolding tools for new elements

## PURPOSE

The `create` namespace is a logical grouping of SPP CLI commands related to Scaffolding tools for new elements. This namespace provides a suite of administrative and operational tools specifically designed to interact with the underlying create subsystems of the framework.

## UNDER THE HOOD ACTIVITY

When invoking commands within the `create` namespace, the CLI router isolates execution to specific modules and classes optimized for Scaffolding tools for new elements. This modular architectural grouping prevents command collisions and ensures that each subsystem operates within its dedicated execution context.

To view the exhaustive details, options, and deep functionality of any specific command within this group, execute `php spp.php man create:<subcommand>`.
