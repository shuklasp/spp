################################################################################
# Application Runtime Configuration — --help
#
# Values here can be read via:
#   \SPP\App::getAppConf('key_name', '--help')
################################################################################

app_name: "--help"
app_type: "legacy"
default_layout: "premium-glass"

# Asset directories (relative to app src)
assets:
  theme-assets: "resources/themes"
  app-assets: "assets"

# Uncomment to enable features:
# debug: true
# cache: false
# auth_required: true