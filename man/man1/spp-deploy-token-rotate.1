.TH SPP-deploy-token-rotate 1 "19 July 2026" "SPP Framework" "SPP CLI Manual"
.SH NAME
deploy:token:rotate
.PP
 \- Rotate the secure deployment gateway token on both local and remote environments with zero downtime
.SH DESCRIPTION
Rotate the secure deployment gateway token on both local and remote environments with zero downtime
.PP
.SH SYNOPSIS
php spp.php deploy:token:rotate [OPTIONS]
.PP
.SH OPTIONS
\(bu `--key=` : Expects a value. Extracted via static analysis.
.br
\(bu `--status` : Boolean flag or option. Extracted via static analysis.
.br
.PP
.SH UNDER THE HOOD
Based on static analysis of the command's source code, invoking this command performs the following operations:
\(bu Performs direct filesystem modifications (create/write/delete).
.br
\(bu Instantiates internal components: deployment, token.
.br
.PP
.PP
